{% extends 'layout.html.twig' %}

{% block content %}
    {% if steps is defined %}
        {% if steps is not iterable %}
            <h1 class="p-5">{{ steps }}</h1>
        {% else %}
            <table class="table table-dark">
                <thead>
                <tr>
                    <th>Les étapes du voyage temporel</th>
                </tr>
                </thead>
                <tbody>
                {% for row in steps|batch(8) %}
                    <tr>
                        {% for step in row %}
                            <td>{{ step }}</td>
                        {% endfor %}
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        {% endif %}
    {% endif %}

{% endblock %}